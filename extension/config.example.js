/**
 * Copy to config.js and fill in. config.js is gitignored.
 *
 * These are the same two values the web app ships in its browser bundle: the
 * anon key grants nothing by itself, since row-level security is the boundary.
 */
export const SUPABASE_URL = 'https://YOUR-PROJECT.supabase.co';
export const SUPABASE_ANON_KEY = 'your-anon-key';
